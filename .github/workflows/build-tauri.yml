name: Build Tauri Apps

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: win32-x64
            os: windows-latest
          - target: linux-x64
            os: ubuntu-latest

    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libfuse2 \
            patchelf \
            ffmpeg

      - name: Install Windows ffmpeg
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco install ffmpeg -y

      - name: Install dependencies
        run: npm ci

      - name: Set tagged release version
        if: startsWith(github.ref, 'refs/tags/v')
        run: npm version ${{ github.ref_name }} --no-git-tag-version --allow-same-version

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Rust tests
        run: cargo test --manifest-path src-tauri/Cargo.toml

      - name: Rust clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings

      - name: Prepare Piper runtime
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run build:runtime -- --target ${{ matrix.target }}

      - name: Smoke test Piper runtime
        run: npm run smoke:runtime -- --runtime runtime/dist/${{ matrix.target }}

      - name: Generate voice demos
        if: matrix.target == 'linux-x64'
        run: npm run build:voice-demos -- --runtime runtime/dist/${{ matrix.target }} --out voice-demos

      - name: Build signed Tauri package
        if: github.event_name != 'pull_request'
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npm run build:tauri

      - name: Build unsigned Tauri package for pull request
        if: github.event_name == 'pull_request'
        run: npm run build:tauri -- --no-sign

      - name: Collect release artifacts
        if: github.event_name != 'pull_request'
        run: npm run collect:tauri-artifacts -- --target ${{ matrix.target }}

      - name: Collect unsigned pull request artifacts
        if: github.event_name == 'pull_request'
        run: npm run collect:tauri-artifacts -- --target ${{ matrix.target }} --skip-signatures

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: audiobook-generator-${{ matrix.target }}
          path: dist-upload/*
          if-no-files-found: error

      - name: Upload voice demos
        if: matrix.target == 'linux-x64'
        uses: actions/upload-artifact@v4
        with:
          name: voice-demos
          path: voice-demos/*
          if-no-files-found: error

      - name: Upload updater signatures
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: updater-signatures-${{ matrix.target }}
          path: dist-upload/*.sig
          if-no-files-found: error

      - name: Publish release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: dist-upload/*

  updater-manifest:
    name: Publish updater manifest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Download updater signatures
        uses: actions/download-artifact@v4
        with:
          pattern: updater-signatures-*
          path: updater-signatures
          merge-multiple: true

      - name: Build updater manifest
        run: npm run build:updater-manifest -- --tag ${{ github.ref_name }} --assets updater-signatures --out updater-signatures/latest.json

      - name: Publish updater manifest
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          fail_on_unmatched_files: true
          files: updater-signatures/latest.json

  pages:
    name: GitHub Pages
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build:site

      - name: Download voice demos
        uses: actions/download-artifact@v4
        with:
          name: voice-demos
          path: dist-site/voice-demos

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist-site

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
